# 架构图索引

本目录存放各项目的系统 / 容器 / 部署架构说明。已定稿层级以正交 **SVG**（spine + bus、zinc 调色）为主视图，Markdown 内嵌展示。

| 项目 | 说明 |
|------|------|
| [4S 店销售 BDC 平台](./proj-super-4s/) | 经销商销售 SaaS：线索 / 外呼 / 活动 / 接待（`context.svg` · `containers.svg` · `deploy.svg`） |
| [AI Coding Agent 编排工作台](./proj-hb-cli/) | hb-cli / agent-desk：本地优先 Agent 编排（`context.svg` · `containers.svg` · `deploy.svg`） |

> 说明：Mermaid 的 `C4Context` / `C4Deployment` 在 GitHub 预览中支持不稳定；有 SVG 的项目以 `context.svg` / `containers.svg` / `deploy.svg` 为主图。语义仍对齐 C4（Context → Container → Deployment）。
> 简历入口等部署图定稿后再加，当前仅维护本目录。
